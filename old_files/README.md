# altie122.xyz

proper development:
1. make sure ports 4321 (blog/astro) and 3000 (www/nextjs) are open
2. `pnpm install` FROM PROJECT ROOT (`./`)
3. `pnpm dev` or `pnpm run dev` FROM PROJECT ROOT (`./`)